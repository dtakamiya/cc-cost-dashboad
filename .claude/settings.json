{
  "permissions": {
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Bash(rm -rf *)",
      "Bash(git push --force*)",
      "Bash(git reset --hard*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$(cat)\" in *' rm -rf /'*|*'--force'*'origin main'*|*'--force'*'origin master'*) echo 'Blocked: destructive command requires manual confirmation' >&2; exit 2;; esac"
          }
        ]
      }
    ]
  }
}
